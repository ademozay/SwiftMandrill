platform :ios, ‘9.0’
use_frameworks!

plugin 'cocoapods-keys'

pod 'Alamofire', '~> 4.8.2’
pod 'ObjectMapper', '~> 3.5.1’


target 'SwiftMandrillTests' do
  pod 'Quick', '~> 2.1.0'
  pod 'Nimble', '~> 8.0.2'

plugin 'cocoapods-keys', {
  :project => "SwiftMandrill",
  :keys => [
    "MANDRILL_API"  
]}

end

